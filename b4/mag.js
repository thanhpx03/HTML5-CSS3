// mang 
//const usertList= new Array("sontv","thienth","dattd");
const usertList=("sontv","thienth","dattd");
//console.log(usertList);

// userList.push("tiennh"); // thêm phần tử cuối mảng
// userList.unshift("huynh"); // thêm phần tử đầu mảng
// userList.pop(); // xoá phần tử cuối mảng
// userList.shift();//xoá phần tử đầu mảng
// userList.splice(2,0,"loantt","huynh")
// userList.splice(1,2)
// userList.splice(1,1,"minhdq")
// userList.splice(0,userList.length)
/*
    sử dụng hàm splice() để thêm sửa xoá dữ liệu trong mảng
    hàm này sẽ nhận vào 3 tham số
        tham số 1: vị trí muốn thay đổi dữ liệu (thêm sửa xoá)
        tham số 2: deleteCount
            thêm mới: để giá trị là 0 
            xoá: để giá trị lớn hơn 0
        tham số 3 trở đi: các giá trị muốn thêm mới
*/
console.log(usertList);